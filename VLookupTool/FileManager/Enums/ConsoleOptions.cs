namespace FileManager.Enums
{
    public enum ConsoleOptions
    {
        options,
        UpOneLevel,
        CurrentPath,
        GoToRoot,
        SelectDirectory,
        ManualFilePath,
        exit
    }
}
